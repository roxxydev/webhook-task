# Default for Prisma CLI and the Prisma VS Code extension when root `.env` is absent.
# Use project root `.env` for your real database URL (see `.env.example`).
DATABASE_URL="postgresql://roxxy@localhost:5432/webhook_task?schema=public"
